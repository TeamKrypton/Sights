<!DOCTYPE html>
<html>
<head>
   <meta charset="utf-8" />
   <title>Vacation Wishlist</title>
   <style type="text/css">
   .images {
       display: flex;
       flex-direction: row;
   }
   </style>
</head>
    <body>
        <script src="https://ajax.googleapis.com/ajax/libs/jquery/2.1.1/jquery.min.js"></script>
        <h1><b>Travel Wishlist</b></h1>
        <form method="POST">
            <!-- <form action="/destinations" method="POST"></form> -->
         <input id="cityInput" type="text" placeholder="name" name="name">
         <button type="submit">Submit</button>
         </form> 
         <ul>
            <% if (cities) { %>
            <% for (var i = 0; i < cities.length; i++) { %>
                <li>
                    <span><%= cities[i].name %></span>
                    <div class="images">
                            <% for (var j=0; j < cities[i].photos.length; j++) { %>
                                <img src="<%=cities[i].photos[j]%>" />
                            <% } %> 
                    </div>
         
                </li>
            <% } %>
            <% } %>
        </ul>
        <script>
            $('html,body').animate({scrollTop: document.body.scrollHeight},"fast");
        </script>
    </body>
</html>